import { render, configure } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';

configure({ testIdAttribute: 'qa-data' });

test('should be render Paciente', () => {});
